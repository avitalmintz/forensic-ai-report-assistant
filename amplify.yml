version: 1
frontend:
  phases:
      build:
            commands:
                    - mkdir -p dist && cp index.html dist/index.html
                        post_build:
                              commands: []
                                artifacts:
                                    baseDirectory: dist/
                                        files:
                                              - '**/*'
                                                cache:
                                                    paths: []
